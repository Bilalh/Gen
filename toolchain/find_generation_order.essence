language Essence 1.3

given N : int(1..20)
letting num_nodes be domain int(1..N)

given Edges : set of (num_nodes, num_nodes)

find ordering : matrix indexed by [num_nodes] of num_nodes

such that
	allDiff(ordering),
	forAll (fro,to) in Edges .
		ordering[to] < ordering[fro]

