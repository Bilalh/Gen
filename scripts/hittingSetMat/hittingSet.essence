language Essence 1.3

given numModels: int(1..1024)
given numSets:   int(1..64)
given sets:      matrix indexed by [int(1..numSets)] of set (maxSize numModels) of int(1..numModels)

find hittingSet : set (maxSize 1024) of int(1..numModels)


such that

forAll i : int(1..numSets) .
    exists v in sets[i] . v in hittingSet

